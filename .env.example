# Copy to .env (Vite picks it up automatically).
# Dev (local backend or docker-compose):
VITE_BACKEND_URL=http://localhost:8000
# Production (Vercel frontend -> Render backend), use your Render URL instead:
# VITE_BACKEND_URL=https://your-render-service-url.onrender.com
